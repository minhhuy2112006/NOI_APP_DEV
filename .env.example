# Local development: explicitly isolated demonstration database and accounts
NOI_MODE=demo
NOI_DEMO_PASSWORD=change-me
# Connected deployment: set NOI_MODE=connected and configure Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Only configure payment credentials after selecting and implementing provider adapter.
